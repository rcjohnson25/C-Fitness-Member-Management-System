/*
Robert Johnson
CpSc 1071, Lab Section #002, Fall 2025
PA3
Due 12/06/2025
This file takes information from a newfile 
*/

//imports
#include <iostream>
#include <cstdlib>
#include <fstream>
#include <vector>
#include <string>
#include <string>
#include <iomanip>
#include <cstring>

//nensures no cin or cerr errors
using namespace std;
void initializeActivityLevels(vector<double> &activityLevels, ifstream &inFile) {
    string line;
    int lineCount = 0;
    if (!inFile.is_open()) {
	cerr << "Error opening file" << endl;
	exit(2);
    }
     //skips over non relevant data 
    while (getline(inFile, line)) {
	lineCount++;
	if (lineCount % 2 == 0) {
		double val = stod(line);
                activityLevels.push_back(val);
	}
    } 

}
